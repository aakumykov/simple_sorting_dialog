pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}

dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenLocal()
        mavenCentral()
        maven{ url "https://jitpack.io" }
    }
}

rootProject.name = "simple_sorting_dialog"
include ':app'
include ':simple_sorting_dialog'
